/**
 * Realtime: install `laravel-echo` and `pusher-js`, set VITE_REVERB_* in `.env`,
 * then instantiate Echo with broadcaster `reverb` per Laravel Reverb docs.
 * Private channels use `routes/channels.php` authorization rules.
 */

export function placeholderEchoBootstrap() {
    return null;
}
